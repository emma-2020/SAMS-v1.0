import { ParentAnalyticsScreen } from '@sams/app';

export const metadata = { title: "My Child's Progress — SAMS" };

export default function ParentAnalyticsPage() {
  return <ParentAnalyticsScreen />;
}
