'use client';

// Parent schedule view — re-uses the full player schedule implementation
// (same data source: /schedule events, same UI logic)
export { default } from '@/app/dashboard/player/schedule/page';
